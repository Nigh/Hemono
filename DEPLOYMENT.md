# 邀请码功能部署指南 - 支持多次使用

## 功能概述

已实现的"加入账本"功能升级版，支持多次使用邀请码：
- 账本拥有者生成邀请码，支持设置使用次数（1-99次）
- 用户通过邀请码加入账本
- 邀请码24小时有效期，支持多次使用
- 完整的使用次数统计和管理

## 新功能特点

### 🎯 核心升级
- **多次使用**：邀请码可设置1-99次使用次数（默认99次）
- **使用统计**：实时显示已使用次数和剩余次数
- **智能控制**：使用次数达上限后自动失效
- **友好界面**：清晰显示使用情况，便于管理

### 🔐 安全特性
- 邀请码格式：ABC-123456（3位大写字母+连字符+6位数字）
- 24小时有效期，过期自动失效
- 使用次数限制，防止滥用
- 权限控制：只有账本拥有者能生成邀请码
- 重复加入检查：用户不能重复加入同一账本

## 待完成步骤

### 1. 创建数据库集合

需要在 PocketBase 管理界面中手动创建 `invitation_codes` 集合：

**集合名称：** `invitation_codes`

**字段列表：**
1. **code** (Text) - 邀请码
   - 最小/最大长度：10
   - 正则：`^[A-Z]{3}-\d{6}$`
   - 必填，唯一

2. **ledger** (Relation) - 关联账本
   - 关联集合：ledgers
   - 最大选择数：1
   - 级联删除：是
   - 必填

3. **created_by** (Relation) - 创建者
   - 关联集合：users
   - 最大选择数：1
   - 级联删除：否
   - 必填

4. **expires_at** (Date) - 过期时间
   - 必填

5. **max_uses** (Number) - 最大使用次数
   - 最小值：1
   - 最大值：99
   - 必填

6. **used_count** (Number) - 已使用次数
   - 最小值：0
   - 默认值：0
   - 可选

7. **created** (Date) - 创建时间（系统字段）

8. **updated** (Date) - 更新时间（系统字段）

**权限设置：**
- **Create Rule:** `created_by = @request.auth.id`
- **View Rule:** 空（允许所有人查看，用于验证邀请码）
- **Update Rule:** `created_by = @request.auth.id`
- **Delete Rule:** `created_by = @request.auth.id`

### 2. 部署后端

```bash
cd /home/engine/project/backend
go build
# 运行服务器
./hemono
```

### 3. 启动前端

```bash
cd /home/engine/project/frontend
npm install
npm run dev
```

## 新功能使用指南

### 📝 生成邀请码（多次使用）
1. 登录账本拥有者账号
2. 打开账本，点击"邀请成员"按钮
3. 设置最大使用次数（1-99，默认99）
4. 点击"生成邀请码"
5. 分享邀请码给朋友

### 🎁 使用邀请码
1. 朋友登录系统
2. 点击头像菜单的"📥 加入账本"
3. 输入邀请码
4. 查看账本信息和使用情况（已用/总数）
5. 确认加入

### 📊 使用情况监控
- 在确认页面显示：**已使用次数/最大使用次数**
- 实时更新使用统计
- 达上限后邀请码自动失效

## API 接口更新

### 生成邀请码（新增 max_uses 参数）
- **POST** `/api/invitations/generate`
- **Body:** `{"ledger_id": "账本ID", "max_uses": 5}`
- **返回:** `{"code": "ABC-123456", "expires_at": "2024-01-01 12:00", "max_uses": 5}`

### 验证邀请码（新增使用统计）
- **GET** `/api/invitations/by-code/{code}`
- **返回:** `{"ledgerId": "账本ID", "ledgerName": "账本名", "maxUses": 5, "usedCount": 2}`

### 加入账本
- **POST** `/api/invitations/join`
- **Body:** `{"code": "邀请码"}`
- **权限：** 已登录用户

## 错误处理增强

### 新增错误类型
- **409**: "邀请码使用次数已达上限"
- **验证**: 使用次数必须在1-99之间
- **统计**: 实时显示剩余可用次数

### 用户体验优化
- 邀请码详情页显示：**使用情况：2/5**
- 生成成功提示："邀请码生成成功，可使用 X 次"
- 清晰的进度指示器

## 部署检查清单

### ✅ 前端功能
- [x] 生成邀请码界面添加使用次数设置
- [x] 邀请码详情显示使用统计
- [x] 邀请码格式验证保持不变
- [x] 错误提示覆盖新增场景

### ✅ 后端API
- [x] 生成接口支持 max_uses 参数
- [x] 验证接口返回使用统计
- [x] 加入接口更新使用计数
- [x] 权限控制保持原有逻辑

### ✅ 数据库
- [x] 创建 invitation_codes 集合
- [x] 添加 max_uses 字段（Number类型）
- [x] 添加 used_count 字段（Number类型）
- [x] 配置相应权限规则

## 测试流程

### 功能测试
1. **生成测试**
   - 设置不同使用次数（1, 5, 99）
   - 验证参数范围限制
   - 确认返回正确的使用次数

2. **使用测试**
   - 输入邀请码查看使用统计
   - 多次使用同一邀请码
   - 验证使用次数递减

3. **边界测试**
   - 使用次数达到上限
   - 邀请码过期处理
   - 权限验证

### 用户体验测试
- 界面清晰显示使用情况
- 错误提示友好准确
- 流程顺畅无卡顿

## 注意事项

1. **数据库兼容性**：需要手动创建集合，支持多次使用功能
2. **版本兼容**：后端API已更新，支持新的使用次数参数
3. **数据迁移**：如需从单次使用升级，需要更新现有邀请码数据
4. **性能考虑**：使用次数统计需要实时更新，确保数据一致性
5. **安全加固**：使用次数限制可有效防止邀请码滥用

---

**升级完成！** 现在邀请码支持多次使用，大大提升了邀请效率和管理灵活性。
